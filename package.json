{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && env\\Scripts\\activate && python app.py\"",
    "install": "concurrently \"cd frontend && npm i\" \"cd backend && python -m venv env && env\\Scripts\\activate && pip install -r requirements.txt\""
  }
}
